[pytest]
; Ensures the project root (where the `bot` package lives) is on
; sys.path when pytest collects tests from the tests/ directory.
; Without this, importing `bot.*` in any test module fails with
; "ModuleNotFoundError: No module named 'bot'".
pythonpath = .
testpaths = tests
asyncio_mode = auto
